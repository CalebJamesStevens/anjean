#include <iostream>
#include <queue> 

namespace Anjean::Core {
  class InputHandler {
    public:
      std::queue<int> inputs;

      // while (!fifo.empty()) {
      //   // Access the front element (the oldest item)
      //   std::cout << "Front element: " << fifo.front() << "\n";
        
      //   // Remove the front element
      //   fifo.pop();
      // }
  }
}